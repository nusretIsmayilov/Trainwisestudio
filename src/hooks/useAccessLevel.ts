/**
 * Re-export useAccessLevel from context for backward compatibility
 * All access logic is centralized in AccessLevelContext
 */
export { useAccessLevel, type AccessLevelState } from '@/contexts/AccessLevelContext';
